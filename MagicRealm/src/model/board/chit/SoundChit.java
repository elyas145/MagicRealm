package model.board.chit;

import model.enums.TileType;

public class SoundChit extends Chit {

	protected SoundChit(TileType tt) {
		super(tt);
	}
	
	//for some reason this gives me an error when i put any code in it. 
	//and i can't access the attributes from the super class.
}
